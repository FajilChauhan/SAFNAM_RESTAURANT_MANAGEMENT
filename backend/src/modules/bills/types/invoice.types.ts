export type InvoiceSummary = {
  invoiceId: string;
  invoiceNumber: string;
  foodTotal: string;
  roomTotal: string;
  extraCharges: string;
  discountTotal: string;
  taxTotal: string;
  grandTotal: string;
  balanceAmount: string;
};
