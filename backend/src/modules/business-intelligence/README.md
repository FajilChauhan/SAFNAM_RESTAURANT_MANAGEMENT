# Business Intelligence Platform

The BI module produces dashboards, charts, and report-ready payloads from existing transactional data. It does not store analytics snapshots; every metric is generated with aggregation queries over bookings, orders, payments, invoices, customers, tables, rooms, and kitchen queues.

Access is restricted to Manager and Admin roles because the module exposes revenue, GST, employee, and customer value analytics.
