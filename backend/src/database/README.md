# database

Owns database clients and connection lifecycle.

Prisma is configured here, while actual models and migrations will be added later.

Use `soft-delete/` helpers in repositories for business records. Authentication session cleanup can still physically delete refresh tokens.
