# types

Stores shared TypeScript types and Express request extensions.

Add types here only when they are shared across multiple modules.

- `api.types.ts`: common API response shapes.
- `pagination.types.ts`: query and pagination contracts.
- `request.types.ts`: request context and authenticated user contracts.
- `express.d.ts`: Express request augmentation.
