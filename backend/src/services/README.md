# services

Stores shared infrastructure services used by multiple modules.

Examples for the future include email, file storage, payment gateways, Redis, and background jobs.

- `upload`: reusable upload preparation.
- `notification`: notification service contract.
- `audit`: audit logging contract.
